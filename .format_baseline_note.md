# Code Formatting Baseline

## Status

Code formatting infrastructure has been set up but baseline formatting has **not yet been applied** to the codebase.

## To Establish Baseline Formatting

Once you have development dependencies installed, run:

```bash
# Install dev dependencies if not already installed
pip install -r requirements-dev.txt

# Format the codebase (core/ and utils/ directories)
make format

# Or manually:
black core/ utils/ comfyui_api.py config.py app.py --line-length 100
```

## What's Already Set Up

✅ **pyproject.toml** - Configuration for Black, Ruff, and pytest
✅ **Makefile** - Convenient commands for formatting, linting, and testing
✅ **requirements-dev.txt** - Black and Ruff are included
✅ **CONTRIBUTING.md** - Complete code style guide
✅ **.gitignore** - Updated to ignore cache and coverage files

## Next Steps

1. **Install dev dependencies**: `pip install -r requirements-dev.txt`
2. **Run baseline formatting**: `make format`
3. **Run linting**: `make lint`
4. **Run tests to verify**: `make test`
5. **Commit formatted code**: `git commit -am "Apply Black formatting baseline"`
6. **Delete this note**: Once baseline is established, delete this file

## Configuration Summary

- **Line length**: 100 characters
- **Target Python**: 3.10+
- **Black**: Formats code automatically
- **Ruff**: Lints and fixes common issues
- **Pytest**: Runs tests with coverage

## Available Make Commands

```bash
make format          # Format code with Black
make lint            # Lint and auto-fix with Ruff
make test            # Run all tests
make coverage        # Generate coverage report
make check           # Run format + lint + test
make clean           # Remove cache files
```

See `CONTRIBUTING.md` for complete documentation.
